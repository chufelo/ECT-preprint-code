# ECT narrative companion

`ECT_companion.tex` is the second publication layer. Its local figures are under `figures/`, its generators/build helper under `scripts/`, and shared preprint figures are resolved explicitly from `../figures/`.

Build from the ECT root with `bash LaTex/companion/scripts/compile_companion.sh`. Historical variants and run artifacts are under `.archive/legacy/`.
