# ECT summary

`ECT_summary.tex` is the third publication layer, downstream of the preprint and companion. Both English and Russian editions use the repository-level canonical bibliography `LaTex/references.bib`; no summary-local bibliography is authoritative.

Build the English summary from the ECT root with `bash LaTex/summary/compile_summary.sh`. Historical summary versions are under `.archive/versions/summary/`.
