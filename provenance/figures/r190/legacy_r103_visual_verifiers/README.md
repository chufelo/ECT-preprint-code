# Historical R103 visual verifier

This verifier is preserved for provenance only.  It expects predecessor
`figures/r103/` assets that are not part of the current public owner graph.
Current publication figures and their hashes are governed by
`FIGURE_REGISTRY.csv`, `FIGURE_REGISTRY.json`, and the public strict registry
verifier.  This historical script is therefore not an active release gate.
