% ============================================================
% CC_D1A_blocks_v1.tex  -- mechanics & wording package (F1-F4)
% Per GPT verdict on CC_O1O5_audit_v1. NO edits performed yet.
% Implementation rule: every OLD string verified count=1 before
% replacement (PYOK gate); 4-pass compile; page count; zero-grep
% checklist at the end of this file.
% Preamble note (Q5): the grading convention "Level A,
% conditional on H_Lambda" was re-verified to carry the explicit
% qualifier at every first mention across all twelve surfaces,
% including the abstract; no Q5 edits are needed.
% ============================================================
\section*{D1-A: mechanics and wording (F1, F2, F3, F4)}

\paragraph{A1 (F1, main tex, L26299--26305).}
OLD: ``are listed as O1, O2, O3 in Section~\ref{subsec:cc\_open}
and together constitute the reformulated OP-$\Lambda$3.''
NEW: ``are listed as the three decoupling-side core tasks
O1--O3 in Section~\ref{subsec:cc\_open}; completed by the
realization-side tasks O4--O5, they together constitute the
reformulated OP-$\Lambda$3.''

\paragraph{A2 (F1, main tex, L45888--45889).}
OLD: ``Reformulated OP-$\Lambda$3 therefore splits into three
technical open problems (O1--O3 of
Section~\ref{subsec:cc\_open}):''
NEW: ``Reformulated OP-$\Lambda$3 therefore comprises the three
decoupling-side core tasks (O1--O3 of
Section~\ref{subsec:cc\_open}), completed by the
realization-side tasks O4--O5:''
(The three enumerated tasks that follow remain unchanged.)

\paragraph{A3 (F1, main tex, L46200--46201).}
OLD: ``(O1--O3 of Section~\ref{subsec:cc\_open}; alternate
label: OP-$\Lambda$3).''
NEW: ``(the decoupling-side core tasks O1--O3 of
Section~\ref{subsec:cc\_open}; with O4--O5 the full reformulated
OP-$\Lambda$3).''
Rationale: the preceding sentence enumerates exactly the three
core tasks, so a bare ``O1--O5'' would mismatch the enumeration.

\paragraph{A4 (F2, cc subsection, Theorem~1(v)).}
Two replacements.
OLD-1: ``\emph{If and only if} the full'' $\to$
NEW-1: ``\emph{If} the full''.
OLD-2: ``and does not source local emergent gravitational
dynamics. If $H_\Lambda$ fails, the UV sensitivity is transferred
into the scalar/radial sector effective potential rather than
into a direct cosmological-constant term; see''
NEW-2: ``and does not source local emergent gravitational
dynamics. Conversely, generic violations of $H_\Lambda$ --- for
example a scalar prefactor $Z(\phi)$ --- reintroduce the
coupling, transferring the UV sensitivity into the
scalar/radial-sector effective potential rather than into a
direct cosmological-constant term; see''
Scope note: the similar conditional sentences in the
$H_\Lambda$-branches paragraph and in Level-A item~3 are correct
descriptions and are NOT touched; OLD-2 includes leading context
to be unique.

\paragraph{A5 (F3, cc subsection, end of the Corollary).}
After ``Neither feeds directly into Object~C in this sector.''
APPEND: ``Beyond-leading corrections to the
ansatz~\eqref{eq:KAB\_cc} are tracked separately as Open
Problem~O5 (Sec.~\ref{subsec:cc\_open}).''

\paragraph{A6 (F4 site 1, App\_CC\_technical).}
OLD: ``$\det(\sqrt{-g}\,g^{\mu\nu}) = -g$ in four dimensions,''
NEW: ``$\det(\sqrt{-g}\,g^{\mu\nu}) = g$ in four dimensions,''

\paragraph{A7 (F4 site 2, App\_CC\_technical, display).}
OLD: ``$\det K^{AB} = -\,g_{\rm eff}$, $\sqrt{-g_{\rm eff}} =
\sqrt{\beta^3(\alpha-\beta)} = \beta^2/c_*$''
NEW: ``$\det K^{AB} = g_{\rm eff} < 0$, $\sqrt{-g_{\rm eff}} =
\sqrt{-\det K^{AB}} = \sqrt{\beta^3(\alpha-\beta)} =
\beta^2/c_*$''
(exact display syntax preserved at implementation).

\paragraph{A8 (F4 site 3 --- audit addendum, cc subsection,
proof outline).} Discovered during D1 preparation: the proof
outline of Theorem~1 repeats the same sign slip.
OLD: ``the densitized-metric identity
$\det(\sqrt{-g}\,g^{\mu\nu}) = -g$ in four dimensions.''
NEW: ``\ldots $= g$ in four dimensions.''
Verified numerically (random Lorentzian metric: both sides
$-0.036448$).

\paragraph{Post-edit zero-grep checklist (D1-A).}
`If and only if' $=0$ in the cc file; `$= -g$' $=0$ in cc and
appendix; `$-\,g_{\rm eff}$' $=0$ in the appendix;
`O1, O2, O3' $=0$; `splits into three technical' $=0$;
`Open Problem~O5' appears exactly twice in the cc file
(definition + new pointer). Compile 4-pass clean; pages
before/after; theorem content and all results unchanged.
