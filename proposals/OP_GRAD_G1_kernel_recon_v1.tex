\documentclass[11pt,a4paper]{article}
\usepackage[T1]{fontenc}\usepackage[utf8]{inputenc}\usepackage{lmodern}
\usepackage[a4paper,margin=2.2cm]{geometry}
\usepackage{amsmath,amssymb}\usepackage{enumitem}\usepackage{booktabs,longtable,array}
\usepackage[hidelinks]{hyperref}
\setlength{\parindent}{0pt}\setlength{\parskip}{0.4em}
\title{\textbf{OP-grad G1: amplitude dependence of $M_G^2$ and $Z_W^{(0)}$
through the recorded induced-stiffness kernel}\\\large Recon v1 ---
proposal-only}
\author{Per reviewer directive after acceptance of the OP-grad anchoring recon}
\date{12 June 2026 --- rev.~1.1 (review wording cautions applied)}
\begin{document}\maketitle

\section*{0. Scope and firewall}
G1 computes, at the level of the recorded structures, the dressing exponents
$\gamma_i=\partial\ln C_i/\partial\ln u\,|_{\rm bare}$ for the two users of the
shared parent coefficient $\kappa_n$: the gravitational stiffness $M_G^2$ and
the weak kinetic normalisation $Z_W^{(0)}$. Everything is conditional on the
recorded kernel (Lemma lem:induced\_stiffness\_kernel, itself Level~B
conditional, a \emph{candidate} route); proposal-only; no preprint edits.

\section{The recorded kernel}
Integrating out the heavy radial mode $\sigma$ at one loop yields the
orientation-stiffness operator (L$\sim$50960--50963)
\[
\frac{\mathcal C_n}{2}\,u^2\,(\partial_B n_A)(\partial^B n^A),
\qquad
\mathcal C_n=\frac{\hat a_{\rm eff}}{16\pi^2 m_\sigma^2},\quad
\hat a_{\rm eff}=\gamma_{\rm fol}\,\mathcal N_{\rm match},
\]
with the chain $\kappa_n\sim(\hat a/16\pi^2)\,u_0^2/m_\sigma^2\sim\phi_0^2$
(eq:MG\_u0) and $M_G^2\sim c_M\kappa_n$ (eq:MG\_condensate). Two recorded
caveats frame everything below: ``the dependence of $\kappa_n$ on
$(u_0,\alpha,\beta)$ remains open (OP-grad, OP2)'', and neither $\mathcal C_n$
nor $\hat a_{\rm eff}$ is determined from the bare action alone. Decisive
structural fact: \emph{the operator as recorded carries the local field
$u(X)^2$}, not the parameter $u_0^2$ --- the stiffness is a locally
re-evaluated composite by construction of the lemma.

\section{The kernel derivative and the $c_G$ consistency selection}
At fixed bare data,
\[
\gamma_{\kappa_n}\;=\;2\;-\;\gamma_{m_\sigma^2}\;-\;\gamma_{\hat a_{\rm eff}},
\]
where the $+2$ is fixed by the recorded $u^2$ prefactor and the remaining
exponents are unrecorded. Taking $\gamma_{\hat a_{\rm eff}}=0$ (matched
normalisation; assumption flag) the cases are:
\renewcommand{\arraystretch}{1.15}\small
\begin{longtable}{@{}p{4.6cm}p{2.2cm}p{2.4cm}p{4.0cm}@{}}
\toprule \textbf{Gap relation} & $\gamma_{m_\sigma^2}$ & $\gamma_{\kappa_n}$ &
\textbf{Implied $c_G$ (if $M_G^2$ tracks $\kappa_n$)} \\ \midrule \endhead
\bottomrule \endfoot
$m_\sigma$ pinned (parameter) & $0$ & $2$ & $-2$ \\
$m_\sigma^2\propto u$ (dimensionally minimal: $[u]={\rm GeV}^2$) & $1$ & $1$ &
$-1$ \emph{(recorded ansatz)} \\
$m_\sigma^2\propto u^2/\phi_0^2$ & $2$ & $0$ & $0$ (no $\varepsilon$-sector) \\
\end{longtable}\normalsize
\textbf{Consistency selection (F7 made concrete):} the recorded
$c_G=-1$ is compatible with the kernel route \emph{if the completion yields}
$m_\sigma^2\propto u$ under the stated assumptions ($\gamma_{\hat a_{\rm
eff}}=0$); a pinned gap would give $c_G=-2$, a quadratically tracking gap would
switch the $\varepsilon$-sector off. This is not derived microphysics: it is a
sharp closure-map \emph{target} --- the recorded ansatz selects a definite gap
relation that the OP-grad completion must either produce or revise (together
with the $|\Delta\ln u|$ normalisation of all thresholds). A
bookkeeping/closure-map result, not an inconsistency.

\section{The $M_G^2$ channel: IR collective response by construction}
The graviton kinetic term descends from the orientation stiffness
(eq:MG\_condensate); the kernel operator carries the local $u(X)^2$. The
gravitational channel is therefore the \emph{locally re-evaluated IR collective
response} of the orientation field --- exactly the first of the two readings
named by the review --- with
\[
\gamma_{M_G^2}=\gamma_{\kappa_n}+\gamma_{c_M}\;\xrightarrow{\;\gamma_{c_M}=0,
\ m_\sigma^2\propto u\;}\;1,
\]
matching the recorded $G_{\rm eff}=G_Ne^{-\beta_\phi\phi}$ dressing. The
$\varepsilon$-sector's gravitational mechanism is thus kernel-realisable with
$\gamma_R=O(1)$, as required.

\section{The $Z_W^{(0)}$ channel: the two readings and the consistency
selection}
The record provides \emph{no} formula $Z_W^{(0)}(u)$: Appendix A.3 states only
the inheritance expectation $Z_W^{(0)}\!\leftarrow\!\kappa_n$, and T1
established that the gauge normalisation is a free EFT matching parameter.
The two readings:

\textbf{(R-local)} $Z_W^{(0)}\propto\kappa_n(u(X))$, locally re-evaluated like
the gravitational channel: $\gamma_{Z_W}=\gamma_{\kappa_n}\in[1,2]$. Then
$\zeta_\alpha\simeq\sin^2\!\theta_W(\phi_0)\,\gamma_{Z_W}\approx0.46$--$0.92$,
giving $|\Delta\alpha/\alpha|\approx0.03$--$0.05$ at $z\sim1.5$ on the band ---
in severe tension (factor $\sim(2$--$4)\times10^{4}$) with the verified sub-ppm
anchor under the local-re-evaluation reading. \emph{This reading is ruled out
within the stated stress-test assumptions.} It is a counterfactual/stress
branch, not the recorded model: no local $Z_W(u)$ formula is recorded.

\textbf{(R-frozen)} $Z_W^{(0)}$ is a UV matching datum evaluated once at
condensate formation (parameter point $u_0,\phi_0$): $\gamma_{Z_W}=0$ up to the
endpoint-log residual already bounded at Step 3. This reading is consistent
with \emph{every} recorded structure: the free-matching-parameter status of the
gauge coupling, the matched-once bookkeeping $G_N=c_*^4/(8\pi M_G^2)$, the
massless Maxwell term, and the selective $\phi$-first closure. It is also
structurally natural under metric-composite mediation plus A4: once matched,
the slow amplitude modulation reaches matter only through
$g^{\rm eff}_{\mu\nu}$.

\textbf{Consistency selection.} The two channels cannot be one and the same
locally re-evaluated coefficient; the anchors force the split: gravitational
channel = IR collective response ($\gamma=O(1)$), gauge normalisation =
UV-frozen matching datum ($\gamma=0$ up to bounded logs). This is a
\emph{selection by consistency}, not yet a microscopic derivation.

\section{G1 verdict and the named remaining derivation}
\begin{enumerate}[leftmargin=1.9em,itemsep=2pt,label=\textbf{V\arabic*.}]
\item The reviewer's decisive question is answered at recon level: the shared
$\kappa_n$ enters the two channels in \emph{different uses} --- the locally
re-evaluated kernel composite for gravity, a frozen parameter-point datum for
the gauge normalisation. The identical-local-coefficient reading is excluded
at the $(2$--$4)\times10^{4}$ level within the stated stress-test assumptions.
\item Bonus (F7): the recorded $c_G=-1$ is compatible with the kernel route if
the completion yields $m_\sigma^2\propto u$ under the stated assumptions
(dimensionally minimal); pinned $m_\sigma$ would give
$c_G=-2$ and rescale $|\Delta\ln u|$, hence all $\gamma$-thresholds, by a
factor $2$. A closure-map decision point, not an inconsistency.
\item Named remaining derivation (the microscopic content of D2 hypotheses
H2/H4): show that the matching integral producing $Z_W^{(0)}$ (and $Z_B^{(0)}$)
at condensate formation is $u$-independent at fixed bare data --- i.e.\ that
the formation-time evaluation point does not follow the subsequent cosmological
excursion. Assumption flags carried: $\gamma_{\hat a_{\rm eff}}=\gamma_{c_M}=0$.
\item All statements are conditional on the Level~B candidate kernel; if the
kernel route is revised, \S2--\S4 must be recomputed.
\end{enumerate}
\end{document}
