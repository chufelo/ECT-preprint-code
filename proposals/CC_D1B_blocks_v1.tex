% ============================================================
% CC_D1B_blocks_v1.tex -- F5 benchmark repair (Route 2 with the
% exact-vs-linearised distinction mandated by GPT).
% NO edits performed yet. Implementation rule: every OLD string
% verified count=1 (PYOK gate); numeric invariance re-checked
% after edit (w_a, 1.106, windows 0.932/0.869).
% Conventions: f_0 = 0.087 (two significant figures) in the
% linearised benchmark, giving w_0 = -0.826 under \simeq, with
% the exact-anchor value r_0 = (1+w_0)/(1-w_0) = 0.095 quoted at
% the single site that pins w_0 = -0.827. Downstream curves are
% invariant because only the product (2/3)f_0^{old} = 2f_0^{new}
% and kappa enter; verified: w_a = -0.745,
% rho_phi(inf)/rho_phi0 = 1.106, coincidence windows
% z = 0.932 (thawing) vs 0.869 (LCDM). The figure fig_w_z_desi
% and DESI's own fit numbers (w_0 ~ -0.83, w_a ~ -0.75 at
% L26417) are untouched.
% ============================================================
\section*{D1-B: F5 repair (exact vs linearised $w$; remove /3;
rescale $f_0$)}

\paragraph{B1 (main tex, eq:w0\_fit block, L21243--21256).}
OLD: ``In terms of the kinetic-to-potential ratio:
$w_0=-1+2\rho_{\rm kin}/(3\rho_{\rm cond})$, with
$\rho_{\rm kin}\equiv\frac12\omega\dot\phi^2$,
$\rho_{\rm cond}\equiv U(\phi)$. This parametrisation is most
useful in the late-time kinetic-subdominant regime \ldots''
NEW: ``In terms of the kinetic-to-potential ratio
$r\equiv\rho_{\rm kin}/\rho_{\rm cond}$ the exact $\phi$-sector
relation and its linearised small-$r$ form read
\begin{equation}
  \label{eq:w0\_fit}
  w_0=\frac{r_0-1}{r_0+1}\;\simeq\;-1+2r_0\,, \qquad
  \rho_{\rm kin}\equiv\tfrac12\omega\dot\phi^2,\quad
  \rho_{\rm cond}\equiv U(\phi).
\end{equation}
The linearised form is the one used in the late-time
kinetic-subdominant regime $\rho_{\rm kin}\ll\rho_{\rm cond}$.
The ratio $r_0$ is not yet derived from bare ECT postulates; no
specific numerical value of $w_0$ can be claimed without a
solved background history~(Level~B/Open).''

\paragraph{B2 (main tex, eq:w\_ect block, L26338--26349).}
OLD bridging+display: ``\ldots with the standard $\phi$-sector
relation $w_\phi=(K-U)/(K+U)$ reducing in the
kinetic-subdominant regime to the expression below.
$w\approx-1+2\rho_{\rm kin}/(3\rho_{\rm cond})$ \ldots''
NEW: ``\ldots: the standard $\phi$-sector relation
$w_\phi=(K-U)/(K+U)$ reduces in the kinetic-subdominant regime
$r\equiv\rho_{\rm kin}/\rho_{\rm cond}\ll1$ to the linearised
expression
\begin{equation}
w \simeq -1 + 2\,\frac{\rho_{\rm kin}}{\rho_{\rm cond}}, \qquad
\rho_{\rm kin}\equiv\tfrac12\omega(\phi)\dot\phi^2,\quad
\rho_{\rm cond}\equiv U(\phi),
\label{eq:w\_ect}
\end{equation}''
(Following no-phantom sentences unchanged: positivity argument
is coefficient-independent.)

\paragraph{B3 (main tex, eq:w\_thaw, L26390--26398).}
OLD: ``\ldots the kinetic fraction decays:
$\rho_{\rm kin}(z)/\rho_{\rm cond}=f_0e^{-\kappa z}$, giving
$w(z)=-1+\frac{2f_0}{3}e^{-\kappa z}$.''
NEW: ``\ldots the kinetic fraction decays. The phenomenological
thawing benchmark adopts
$\rho_{\rm kin}(z)/\rho_{\rm cond}=f_0\,e^{-\kappa z}$, giving,
in the linearised small-$r$ regime,
\begin{equation}
w(z) \simeq -1 + 2 f_0\, e^{-\kappa z}\,.
\label{eq:w\_thaw}
\end{equation}''

\paragraph{B4 (main tex, L26482--26484).}
OLD: ``with $f_0\approx0.26$ and $\kappa\approx4.3$
(DR1-anchored legacy calibration,''
NEW: ``with $f_0\approx0.087$ and $\kappa\approx4.3$
(DR1-anchored legacy calibration of the linearised benchmark,''

\paragraph{B5 (main tex, eq:r\_ratio\_thaw L26528 + values
L26534).}
OLD exponent: ``$\exp[-2f_0\int_0^z e^{-\kappa z'}/(1+z')dz']$''
NEW exponent: ``$\exp[-6f_0\int_0^z e^{-\kappa z'}/(1+z')dz']$''
(from continuity, $3(1+w)=6f_0e^{-\kappa z}$; numerically
$6f_0^{\rm new}=2f_0^{\rm old}$, so all values stand).
OLD values line: ``legacy benchmark
$(f_0,\kappa)\approx(0.26,4.3)$ yields'' $\to$ NEW:
``$(f_0,\kappa)\approx(0.087,4.3)$ yields''; the quoted 1.11,
the $11\%$ remark, and the windows $0.93/0.87$ are unchanged
(re-verified).

\paragraph{B6 (main tex, L27117--27118).}
OLD: ``benchmark $(f_0,\kappa)\approx(0.26,4.3)$ is only
marginally wider'' $\to$ NEW: ``benchmark
$(f_0,\kappa)\approx(0.087,4.3)$ is only marginally wider''.

\paragraph{B7 (main tex, L45628--45632; the exact-anchor
site).}
OLD: ``\ldots if the ratio of kinetic to condensate energy
density satisfies $\rho_{\rm kin}/\rho_{\rm cond}\approx0.26$
(DR1-anchored legacy benchmark).''
NEW: ``\ldots if the ratio of kinetic to condensate energy
density satisfies $\rho_{\rm kin}/\rho_{\rm cond}\approx0.087$
in the linearised thawing benchmark (exactly,
$r_0=(1+w_0)/(1-w_0)\approx0.095$ for $w_0=-0.827$;
DR1-anchored legacy benchmark).''

\paragraph{B8 (main tex, dark-energy table row L23241).}
OLD: ``$w_0=-1+2\rho_{\rm kin}/(3\rho_{\rm cond})$ grounded by
FRW reduction \ldots''
NEW: ``exact $w_0=(r_0-1)/(r_0+1)$, linearised
$w_0\simeq-1+2\rho_{\rm kin}/\rho_{\rm cond}$, grounded by FRW
reduction \ldots'' (rest of row unchanged).

\paragraph{B9 (cc subsection, L370--386).}
OLD: display $w_0=-1+2\rho_{\rm kin}/(3\rho_{\rm cond})$ +
``upon identifying \ldots The ECT value $w_0\approx-0.83$ (for
$\rho_{\rm kin}/\rho_{\rm cond}\approx0.26$) is therefore an
explicit instance \ldots''
NEW: ``\ldots provides the formal grounding for the dark-energy
relation of Sec.~\ref{sec:mp\_late\_cosmology}: with
$\rho_{\rm kin}=\tfrac12 Z_q\dot q^2$ and
$\rho_{\rm cond}=V_{\rm IR}(q)$, the exact slow-branch relation
is $w_0=(r_0-1)/(r_0+1)$, $r_0\equiv\rho_{\rm kin}/
\rho_{\rm cond}$, linearising to
\begin{equation}
w_0 \simeq -1 + \frac{2\rho_{\rm kin}}{\rho_{\rm cond}}\,.
\end{equation}
The benchmark value $w_0\approx-0.83$ corresponds to
$r_0\approx0.087$ in the linearised thawing benchmark (exactly,
$r_0=(1+w_0)/(1-w_0)\approx0.095$), and is therefore an explicit
instance of the general slow-branch reduction.''
(The following DESI-comparability sentence is unchanged.)

\paragraph{B10 (App\_CC\_technical, app:frw\_cc).}
OLD: ``reproducing the main-text formula
$w_0=-1+2\rho_{\rm kin}/(3\rho_{\rm cond})$ upon
identification''
NEW: ``matching the linearised main-text relation
$w_0\simeq-1+2\rho_{\rm kin}/\rho_{\rm cond}$ upon
identification''

\paragraph{Post-edit checklist (D1-B).}
Zero-greps across all three files: `$3\rho_{\rm cond}$' and the
mathrm variant $=0$; `$\frac{2f_0}{3}$' $=0$; `$(0.26,4.3)$'
$=0$; `$f_0 \approx 0.26$' $=0$; `$\approx 0.26$ (DR1' $=0$.
Presence: linearised relation appears with $\simeq$ at all five
formula sites; $r_0\approx0.095$ exactly once (B7) plus once in
the cc subsection (B9); $0.087$ at five sites. Numeric re-check
after edit: $w_a=-0.745$, $\rho_\phi(\infty)/\rho_{\phi0}=1.106$,
windows $0.932/0.869$ reproduced from the NEW displayed formulas.
Compile 4-pass clean; pages before/after; figure untouched.
