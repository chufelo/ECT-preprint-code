COMPANION LINE-LEVEL SYNC - 2026-06-11 (user-directed; propagation of
GPT-approved main-article content; recommend GPT re-check at next contact)

Audit result first: the companion was already clean on most fronts - the
/3 w0 formula, the 0.26 ratio, f_0, O1-O3 enumerations, the iff wording
and the densitized-metric sign slip NEVER existed in it (0 hits each);
the cc passages already carry the Object A / B(H_Lambda-conditional) /
C structure verbatim-equivalent to the main text. cc edits: none needed.

Twelve text edits applied (all count=1 gated):
 T1/T2  OP-grad (intro + open-problems item): added the OP3.1a-c
        decomposition and the localisation of the remaining gap to the
        constraint-relaxation/coarse-graining bridge; computed candidate
        bridges stated as excluded.
 T3-T6  Hypercharge/interlock layer (charge-quantisation section tail,
        electric-charge open item, OP-GUT3 item, anomaly-architecture
        paragraph): recorded Z6 colour-interlock congruence, fractional
        pattern Q = -t/3 mod 1 per colour class, residual freedom =
        explicitly characterised discrete family; every instance carries
        the mandatory "consistency requirement on completions, not a
        derivation" guard.
 T7-T10, T12  Benchmark discipline for w0 = -0.83 (five sites incl. the
        former "ECT predicts" sentence, both principal-results lists,
        the predictions item, and the summary-table row): now uniformly
        "calibrated thawing benchmark", explicitly "a calibration, not
        a first-principles prediction" at the narrative site. Enforces
        the user's standing rule (benchmarks never enter derivations).
 T11    New Part-I principal-results item: discrete-symmetry / anomaly /
        flavour architecture (A/B; A; B/Open) - restores text<->figure
        consistency with the updated derivation-logic graph.

Figure: fig_partI_derivation_logic_pop.{pdf,png} regenerated from the
new portrait source scripts/fig_partI_vertical_v2_pop.gv (= today's
fig_partI_vertical_v2.gv with section references stripped per the pop
convention; digraph renamed PartIpop). Geometry 12.3 x 15.3 in portrait;
old April artifacts preserved in LaTex/backup/ as
fig_partI_derivation_logic_pop_PRE_SYNC_2026-06-11.{pdf,png}.

Gates: companion compile 3-pass clean (0 errors / 0 undef refs / 0 undef
cites / 0 multiply / 0 float-too-large); pages 104 -> 104. Companion tex
backup: LaTex/backup/ECT_companion_BACKUP_2026-06-11_pre_sync.tex.

Known deferred (flagged, not done): Part II/III/IV pop graphs in the
companion still reflect their April sources while the main-article
counterparts were regenerated on Jun 10 - a follow-up regeneration pass
mirroring today's Part-I treatment is recommended. Zenodo companion DOI
not updated (manual author step).
